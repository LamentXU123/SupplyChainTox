codecov:
  require_ci_to_pass: true

coverage:
  status:
    project:
      default:
        target: auto
        threshold: 2%
        if_not_found: error
    patch:
      default:
        target: auto
        threshold: 5%
        if_not_found: error

ignore:
  - "tests/*"
  - "**/__pycache__/*"
